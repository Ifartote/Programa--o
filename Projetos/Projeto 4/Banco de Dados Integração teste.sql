CREATE DATABASE projeto_integração;
USE projeto_integração;
CREATE TABLE integração (
  id INT AUTO_INCREMENT PRIMARY KEY,
  nome VARCHAR(100),
  idade INT,
  altura DECIMAL(4,2),
  status VARCHAR(50)
);
show tables;
